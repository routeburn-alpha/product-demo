# Per-worktree scratch logs (npm install, build, dev-server output)
/logs/

# Per-worktree local files (agent identity, local bug log)
CLAUDE.local.md
bug-tracker.md

# Studio AI MCP config — copied from .mcp.json.example, may contain a bearer token
.mcp.json

# OS
.DS_Store
Thumbs.db
